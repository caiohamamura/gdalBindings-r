# Makefile for GEDI simulator tools
PKG_CPPFLAGS = $(shell pkg-config --cflags gdal proj) -I.

PKG_LIBS=$(shell pkg-config --libs gdal proj)


OBJECTS = rasterizeHDF.o RcppExports.o

all: $(SHLIB)
$(SHLIB): $(OBJECTS)

.PHONY: all deps
